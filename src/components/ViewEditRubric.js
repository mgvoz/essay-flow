import React from 'react';
import { useHistory } from 'react-router-dom';

export default function ViewEditRubric({ rubrics }) {
	const history = useHistory();
	return <div className='page-container'></div>;
}
